{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tidus.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tidus.labels" . | nindent 4 }}
{{- end }}
