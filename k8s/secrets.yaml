# TEMPLATE — do not commit real secrets.
# Populate values with: kubectl create secret generic tidus-secrets --from-env-file=.env -n tidus
# Or use an external secrets operator (AWS Secrets Manager, Vault, etc.)
apiVersion: v1
kind: Secret
metadata:
  name: tidus-secrets
  namespace: tidus
  labels:
    app.kubernetes.io/name: tidus
    app.kubernetes.io/component: api
type: Opaque
stringData:
  OPENAI_API_KEY: ""
  ANTHROPIC_API_KEY: ""
  GOOGLE_API_KEY: ""
  MISTRAL_API_KEY: ""
  DEEPSEEK_API_KEY: ""
  XAI_API_KEY: ""
  MOONSHOT_API_KEY: ""
  OIDC_ISSUER_URL: ""
  OIDC_CLIENT_ID: ""
  OIDC_CLIENT_SECRET: ""
  POSTGRES_PASSWORD: "changeme"
